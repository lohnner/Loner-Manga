import { mkdir, readFile, writeFile } from "node:fs/promises";

const output = new URL("../Mangás/Vagabond/", import.meta.url);
const volumes = JSON.parse(await readFile(new URL("vagabond-volumes.json", import.meta.url), "utf8"));
const baseUrl = "https://lohnner.github.io/Loner-Manga/Mang%C3%A1s/Vagabond";
await mkdir(output, { recursive: true });

const formatDate = (iso) => new Intl.DateTimeFormat("pt-BR", { day: "numeric", month: "long", year: "numeric", timeZone: "UTC" }).format(new Date(`${iso}T00:00:00Z`));
const arcName = (chapter) => chapter <= 21 ? "Takezō" : chapter <= 35 ? "Primeiro Yoshioka" : chapter <= 78 ? "Hōzōin" : chapter <= 105 ? "Yagyū" : chapter <= 127 ? "Baiken" : chapter <= 179 ? "Kojirō" : chapter <= 242 ? "Segundo Yoshioka" : chapter <= 301 ? "Pós-Yoshioka" : "Agricultura";
const shellStart = (title, description, canonical, image) => `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"/><meta name="viewport" content="width=device-width,initial-scale=1"/><title>${title} - Loner Mangá</title><link rel="icon" href="../../favicon.png"/><meta name="theme-color" content="#0a0d14"/><meta name="description" content="${description}"/><link rel="canonical" href="${canonical}"/><meta property="og:type" content="article"/><meta property="og:site_name" content="Loner Mangá"/><meta property="og:title" content="${title} - Loner Mangá"/><meta property="og:description" content="${description}"/><meta property="og:image" content="${image}"/><meta property="og:url" content="${canonical}"/><meta name="twitter:card" content="summary_large_image"/><link rel="stylesheet" href="../../styles.css" data-loner-cache-bust/><script src="../../boot.js" data-loner-app="../../script.js"></script></head><body><div class="site-shell"><aside class="sidebar"><a class="brand sidebar-fallback-brand" href="../../index.html"><span class="brand-mark">LM</span><span><strong>Loner Mangá</strong><small>Arquivo de mangás</small></span></a><nav class="wiki-nav sidebar-fallback-nav"><a href="../../index.html">HOME</a><a href="../../Universos/universos.html">Mangás</a><a href="../../pesquisar.html">Pesquisar</a><a href="../../ranking.html">Ranking de Usuários</a></nav></aside><div class="content-shell"><header class="topbar"></header>`;
const shellEnd = `<footer class="site-footer"><p>Loner Mangá é um projeto local de catalogação de mangás.</p></footer></div></div></body></html>\n`;

const cards = volumes.map(({ number, releaseDate }) => `<a class="volume-card" href="vagabond-${number}-${releaseDate.slice(0, 4)}.html"><img src="Vagabond%20%23${number}.png" alt="Capa do volume ${number} de Vagabond" loading="lazy"/><span>Volume ${number}</span></a>`).join("");
const description = "Mangá histórico de Takehiko Inoue inspirado na vida do espadachim Miyamoto Musashi e no romance Musashi, de Eiji Yoshikawa.";
const arcs = [["Takezō",1,21],["Primeiro Yoshioka",22,35],["Hōzōin",36,78],["Yagyū",79,105],["Baiken",106,127],["Kojirō",128,179],["Segundo Yoshioka",180,242],["Pós-Yoshioka",243,301],["Agricultura",302,322]];
const arcRows = arcs.map(([name,start,end], index) => `<tr><td>${index + 1}</td><td>${name}</td><td>${start}</td><td>${end}</td></tr>`).join("");
const dialog = `<dialog class="arcs-dialog" id="vagabondArcsDialog" aria-labelledby="vagabondArcsDialogTitle"><div class="arcs-dialog-header"><h2 id="vagabondArcsDialogTitle">Arcos de Vagabond</h2><button class="dialog-close" type="button" data-dialog-close aria-label="Fechar">&times;</button></div><div class="arcs-table-wrap"><table class="data-table arcs-table"><thead><tr><th>Nº</th><th>Arco</th><th>Início</th><th>Fim</th></tr></thead><tbody>${arcRows}</tbody></table></div><p>Os capítulos 323 a 327 pertencem ao arco Hosokawa, mas permanecem sem compilação em volume físico.</p></dialog>`;
const main = `${shellStart("Vagabond", description, `${baseUrl}/vagabond.html`, `${baseUrl}/Vagabond%20%231.png`)}<main class="wiki-page universe-layout"><article class="article"><nav class="breadcrumbs"><a href="../../index.html">Início</a><span>/</span><a href="../../Universos/universos.html">Mangás</a><span>/</span><span>Vagabond</span></nav><header class="article-header"><h1>Vagabond</h1><p class="lead">${description}</p></header><section class="content-band"><h2>Resumo do Mangá</h2><p>No Japão do início do século XVII, o impetuoso Shinmen Takezō sobrevive à Batalha de Sekigahara e inicia uma jornada de transformação. Ao adotar o nome Miyamoto Musashi, ele procura compreender o verdadeiro significado de ser invencível sob o sol.</p><p>A obra combina duelos, filosofia e amadurecimento em uma releitura visualmente marcante da trajetória de Musashi e de seu rival Sasaki Kojirō.</p></section><section class="content-band"><h2>Informações do Mangá</h2><table class="data-table"><tbody><tr><th>Autor</th><td>Takehiko Inoue</td></tr><tr><th>Editora</th><td>Kodansha</td></tr><tr><th>Publicação</th><td>1998 – em hiato desde 2015</td></tr><tr><th>Volumes</th><td>37</td></tr><tr><th>Arcos</th><td><button class="text-action" type="button" data-arcs-dialog="vagabondArcsDialog">Ver Completo</button></td></tr></tbody></table></section><section class="content-band"><h2>Volumes</h2><div class="volume-gallery">${cards}</div></section>${dialog}</article></main>${shellEnd}`;
await writeFile(new URL("vagabond.html", output), main);

for (const { number, releaseDate, chapters } of volumes) {
  const year = releaseDate.slice(0, 4), [start, end] = chapters;
  const title = `Vagabond / Volume ${number}`, file = `vagabond-${number}-${year}.html`, cover = `Vagabond%20%23${number}.png`;
  const summary = `Volume ${number} de Vagabond, de Takehiko Inoue, reunindo os capítulos ${start} a ${end}.`;
  const page = `${shellStart(title, summary, `${baseUrl}/${file}`, `${baseUrl}/${cover}`)}<main class="wiki-page"><article class="article"><nav class="breadcrumbs"><a href="../../index.html">Início</a><span>/</span><a href="../../Universos/universos.html">Mangás</a><span>/</span><a href="vagabond.html">Vagabond</a><span>/</span><span>Volume ${number}</span></nav><header class="article-header"><h1>${title}</h1><p class="lead">${summary}</p></header><section class="content-band"><h2>Resumo</h2><p>Este volume desenvolve o arco ${arcName(start)}, acompanhando a evolução de Musashi e os encontros que desafiam sua força, sua técnica e sua compreensão do caminho da espada.</p><p>A narrativa une ação, introspecção e o retrato do Japão feudal na jornada do espadachim.</p></section><section class="content-band"><h2>Dados do Volume</h2><table class="data-table"><tbody><tr><th>Título</th><td>Vagabond #${number}</td></tr><tr><th>Ano</th><td>${year}</td></tr><tr><th>Publicação</th><td>${formatDate(releaseDate)}</td></tr><tr><th>Editora</th><td>Kodansha</td></tr><tr><th>Roteiro</th><td>Takehiko Inoue</td></tr><tr><th>Arte</th><td>Takehiko Inoue</td></tr><tr><th>Série</th><td><a href="vagabond.html">Vagabond</a></td></tr><tr><th>Capítulos</th><td>${start} a ${end}</td></tr><tr><th>Páginas</th><td>224 páginas</td></tr></tbody></table></section></article><aside class="infobox"><h2>${title}</h2><img src="${cover}" alt="Capa do volume ${number} de Vagabond"/><table><tbody><tr><th>Universo</th><td>Vagabond</td></tr><tr><th>Série</th><td>Vagabond</td></tr><tr><th>Editora</th><td>Kodansha</td></tr><tr><th>Publicação</th><td>${formatDate(releaseDate)}</td></tr></tbody></table></aside></main>${shellEnd}`;
  await writeFile(new URL(file, output), page);
}

const catalogPath = new URL("../catalogo.js", import.meta.url);
let catalog = await readFile(catalogPath, "utf8");
const startMarker = "  // VAGABOND_COMPLETE_CATALOG", endMarker = "  // VAGABOND_COMPLETE_CATALOG_END";
const entries = volumes.map(({ number, releaseDate, chapters }) => { const year = releaseDate.slice(0,4); return `  { tipo: "hq", id: "vagabond-${number}-${year}", title: "Vagabond / Volume ${number}", shortTitle: "Vagabond / Volume ${number}", universe: "Vagabond", series: "Vagabond", href: "Mangás/Vagabond/vagabond-${number}-${year}.html", cover: "Mangás/Vagabond/Vagabond%20%23${number}.png", pageCount: 224, xpReward: 224, fileName: "vagabond-${number}-${year}.html", publicationDate: "${formatDate(releaseDate)}", chapters: "${chapters[0]} a ${chapters[1]}", keywords: ["Vagabond", "Volume ${number}", "Miyamoto Musashi", "Takehiko Inoue", "Kodansha", "samurai"] },`; }).join("\n");
const block = `${startMarker}\n${entries}\n${endMarker}\n`;
const existing = /  \/\/ VAGABOND_COMPLETE_CATALOG[\s\S]*?  \/\/ VAGABOND_COMPLETE_CATALOG_END\n/;
catalog = existing.test(catalog) ? catalog.replace(existing, block) : catalog.replace("  // CAPTAIN_TSUBASA_COMPLETE_CATALOG", `${block}  // CAPTAIN_TSUBASA_COMPLETE_CATALOG`);
await writeFile(catalogPath, catalog);

const sitemapPath = new URL("../sitemap.xml", import.meta.url);
let sitemap = await readFile(sitemapPath, "utf8");
sitemap = sitemap.replace(/\s*<url>\s*<loc>[^<]*Vagabond[^<]*<\/loc>\s*<\/url>/g, "");
const urls = [`  <url><loc>${baseUrl}/vagabond.html</loc></url>`, ...volumes.map(({ number, releaseDate }) => `  <url><loc>${baseUrl}/vagabond-${number}-${releaseDate.slice(0,4)}.html</loc></url>`)].join("\n");
sitemap = sitemap.replace("</urlset>", `${urls}\n</urlset>`);
await writeFile(sitemapPath, sitemap);
console.log("Vagabond: 37 volumes, capítulos 1 a 322, catálogo e sitemap preparados.");
